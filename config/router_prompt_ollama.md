Classify the user query. Reply with one word only: JQL or GENERAL.

JQL    — the query asks about Jira issues, tickets, bugs, tasks, stories, epics, sprints, or projects. Queries that also ask for a chart (bar, pie, line, stacked bar, etc.) alongside Jira data are still JQL.
GENERAL — the query is about anything else (greetings, general knowledge, opinions, tool comparisons, etc.)

Examples:
- "list open bugs in KAFKA" → JQL
- "show tickets assigned to John" → JQL
- "which epics in sprint 42 are still open?" → JQL
- "list 1000 issues with major priority from project kafka, create a stacked bar chart" → JQL
- "show bugs by assignee and status as a bar chart" → JQL
- "pie chart of issues by priority in HADOOP" → JQL
- "how many states are there in India?" → GENERAL
- "hello" → GENERAL
- "what is JQL?" → GENERAL
- "compare Jira to GitHub Issues" → GENERAL

Query: {query}

Output (JQL or GENERAL):
